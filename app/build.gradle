buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.3.1" // latest AGP
        // Add other classpaths here if needed
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}